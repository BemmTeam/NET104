@using Microsoft.AspNetCore.Identity
@using ASMMAIN.Models

@inject SignInManager<User> SignInManager
@inject UserManager<User> UserManager

<div class="header_nav">
    <ul class="header_nav-list">
@if (SignInManager.IsSignedIn(User))
{
    <li class="header_nav-list-item header_user">
       <img src="/images/unnamed.png" alt="" class="header_user-avt" />


            <div id="manage" class="header_user-name">Hello @UserManager.GetUserName(User)!
            <div class="header_user-menu">
                <ul class="header_user-menu-list">
                    @if(User.IsInRole(RoleName.Administrator)){ 
                    <li class="header_user-menu-item"><a asp-action="Index" asp-area="Database" asp-controller="Dbmanage">Vào trang quản lý</a></li>
                        
                    }
                    <li class="header_user-menu-item"><a asp-action="Index" asp-area="Manage" asp-controller="Account">Thông tin tài khoản</a></li>
                    <li class="header_user-menu-item"><a asp-action="Signout" asp-controller="Account" asp-route-returnUrl = "@Url.Action("Index", "Home", new { area = "" })">Đăng xuất</a></li>
                </ul>
            </div>
            </div>

     

    </li>
    @* <li class="header_nav-list-item">
        <form id="logoutForm" class="form-inline" asp-page="/Account/Logout" asp-route-returnUrl="@Url.Action("Index", "Home", new { area = "" })">
            <button id="logout" type="submit" class="nav-link btn btn-link text-dark">Logout</button>
        </form>
    </li> *@
}
else
{
    <li class="header_nav-list-item">
        <a class="nav-link text-dark" id="register" asp-action="signup" asp-controller="account" asp-route-returnurl = "/">Register</a>
    </li>
    <li class="header_nav-list-item">
        <a class="nav-link text-dark" id="login"  asp-action="signin" asp-controller="account" asp-route-returnurl = "/">Login</a>
    </li>
    
}
</ul>
</div>
